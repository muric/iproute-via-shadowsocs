[Unit]
Description=route shadow
After=multi-user.target
[Service]
Type=oneshot
Environment="IPROUTE_GIT_DIR=/home/user/iproute-via-shadowsocs"
WorkingDirectory=${IPROUTE_GIT_DIR}
ExecStart=srtunectl
[Install]
WantedBy=multi-user.target
